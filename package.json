{  
  "name": "opensid-frontend",  
  "version": "1.0.0",  
  "description": "OpenSID Frontend Build Pipeline",  
  "private": true,  
  "scripts": {  
    "dev": "vite",  
    "build": "vite build",  
    "preview": "vite preview"  
  },  
  "devDependencies": {  
    "vite": "^5.0.0"  
  }  
} 
